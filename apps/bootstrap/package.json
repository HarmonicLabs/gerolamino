{
  "name": "bootstrap-server",
  "private": true,
  "type": "module",
  "module": "src/cli.ts",
  "scripts": {
    "type-check": "tsgo --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "bootstrap": "workspace:*",
    "codecs": "workspace:*",
    "ledger": "workspace:*",
    "lsm-ffi": "workspace:*",
    "storage": "workspace:*"
  }
}
